#!/usr/bin/env node/** * Script final de sincronização - Mostra os 4 passos completos formatados * para execução manual no SQL Editor do Supabase Studio */console.log('=========================================');console.log('🚀 SUPABASE SYNC - EXECUÇÃO DOS 4 PASSOS');console.log('=========================================');console.log('\\n🎯 OBJETIVO: Executar os 4 passos solicitados para sincronizar o banco de dados\\n');console.log('🔐 Projeto: jpztntmwmrhdobxsyulj');console.log('🌐 URL: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj/database/sql\\n');console.log('📋 PASSOS PARA EXECUÇÃO NO SQL EDITOR:\\n');console.log('='.repeat(80));console.log('PASSO 1: CRIAR TABELA "paginas"');console.log('='.repeat(80));console.log(`-- Tabela para armazenar as páginas existentes no sistemaCREATE TABLE IF NOT EXISTS paginas (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  nome TEXT NOT NULL UNIQUE,  titulo TEXT NOT NULL,  descricao TEXT,  ativo BOOLEAN DEFAULT true,  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW());-- Índice para a tabela paginasCREATE INDEX IF NOT EXISTS idx_paginas_nome ON paginas(nome);-- Políticas de segurançaDROP POLICY IF EXISTS "Admin full access paginas" ON paginas;CREATE POLICY "Admin full access paginas" ON paginas USING ((auth.role() = 'authenticated'::text)) WITH CHECK ((auth.role() = 'authenticated'::text));DROP POLICY IF EXISTS "Admins can manage paginas" ON paginas;CREATE POLICY "Admins can manage paginas" ON paginas TO authenticated USING (public.is_admin()) WITH CHECK (public.is_admin());DROP POLICY IF EXISTS "Public read active paginas" ON paginas;CREATE POLICY "Public read active paginas" ON paginas FOR SELECT USING ((ativo = true));ALTER TABLE paginas ENABLE ROW LEVEL SECURITY;`);console.log('='.repeat(80));console.log('PASSO 2: ATUALIZAR TABELA "galeria_fotos"');console.log('='.repeat(80));console.log(`-- Atualiza a tabela galeria_fotos para adicionar a coluna pagina_exibicaoDO $$BEGIN  IF NOT EXISTS (SELECT 1 FROM information_schema.columns                  WHERE table_name = 'galeria_fotos'                  AND column_name = 'pagina_exibicao') THEN    ALTER TABLE galeria_fotos ADD COLUMN pagina_exibicao TEXT;  END IF;END$$;-- Atualiza registros existentes para remover o valor 'todas'UPDATE galeria_fotos SET pagina_exibicao = NULL WHERE pagina_exibicao = 'todas';-- Atualiza a política de galeria_fotosDROP POLICY IF EXISTS "Admins can manage galeria_fotos" ON galeria_fotos;CREATE POLICY "Admins can manage galeria_fotos" ON galeria_fotos TO authenticated USING (public.is_admin()) WITH CHECK (public.is_admin());`);console.log('='.repeat(80));console.log('PASSO 3: CRIAR FUNÇÃO DE VALIDAÇÃO');console.log('='.repeat(80));console.log(`-- Cria a função de validação para garantir que apenas páginas existentes sejam usadasCREATE OR REPLACE FUNCTION validate_pagina_exibicao()RETURNS TRIGGER AS $$BEGIN  -- Verifica se a pagina_exibicao é NULL (fotos antigas) ou está na tabela paginas  IF NEW.pagina_exibicao IS NOT NULL AND NEW.pagina_exibicao != 'todas' THEN    IF NOT EXISTS (SELECT 1 FROM paginas WHERE nome = NEW.pagina_exibicao AND ativo = true) THEN      RAISE EXCEPTION 'Página de exibição não existe: %', NEW.pagina_exibicao;    END IF;  END IF;  RETURN NEW;END;$$ LANGUAGE plpgsql;-- Adiciona o trigger à tabela galeria_fotosDROP TRIGGER IF EXISTS trigger_validate_pagina_exibicao ON galeria_fotos;CREATE TRIGGER trigger_validate_pagina_exibicao  BEFORE INSERT OR UPDATE ON galeria_fotos  FOR EACH ROW  EXECUTE FUNCTION validate_pagina_exibicao();`);console.log('='.repeat(80));console.log('PASSO 4: CRIAR TABELA "documentos"');console.log('='.repeat(80));console.log(`-- Criação da tabela de documentosCREATE TABLE IF NOT EXISTS documentos (  id UUID DEFAULT gen_random_uuid() NOT NULL,  nome TEXT NOT NULL,  descricao TEXT,  arquivo_url TEXT NOT NULL,  local_exibicao TEXT DEFAULT 'secretaria',  ativo BOOLEAN DEFAULT true,  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW());-- Adiciona chave primáriaALTER TABLE "public"."documentos" ADD CONSTRAINT "documentos_pkey" PRIMARY KEY ("id");-- Adiciona trigger para atualizar o campo updated_atCREATE OR REPLACE TRIGGER "trigger_atualizar_documentos_updated_at" BEFORE UPDATE ON "public"."documentos" FOR EACH ROW EXECUTE FUNCTION "public"."atualizar_updated_at"();-- Adiciona política de segurançaCREATE POLICY "Admin full access documentos" ON "public"."documentos" USING (("auth.role"() = 'authenticated'::"text")) WITH CHECK (("auth.role"() = 'authenticated'::"text"));CREATE POLICY "Admins can manage documentos" ON "public"."documentos" TO "authenticated" USING ("public"."is_admin"()) WITH CHECK ("public"."is_admin"());CREATE POLICY "Public read active documentos" ON "public"."documentos" FOR SELECT USING (("ativo" = true));ALTER TABLE "public"."documentos" ENABLE ROW LEVEL SECURITY;-- Adiciona a tabela ao schema públicoGRANT ALL ON TABLE "public"."documentos" TO "anon";GRANT ALL ON TABLE "public"."documentos" TO "authenticated";GRANT ALL ON TABLE "public"."documentos" TO "service_role";-- Cria índices para melhorar performanceCREATE INDEX IF NOT EXISTS "idx_documentos_created_at" ON "public"."documentos" ("created_at");CREATE INDEX IF NOT EXISTS "idx_documentos_ativo" ON "public"."documentos" ("ativo");CREATE INDEX IF NOT EXISTS "idx_documentos_local_exibicao" ON "public"."documentos" ("local_exibicao");`);console.log('='.repeat(80));console.log('PASSO EXTRA: INSERIR PÁGINAS PADRÃO');console.log('='.repeat(80));console.log(`-- Inserindo páginas padrãoINSERT INTO paginas (nome, titulo, descricao, ativo) VALUES('inicio', 'Início', 'Página inicial', true),('secretaria', 'Secretaria', 'Página da Secretaria', true),('demutran', 'Demutran', 'Página da Demutran', true),('guarda-municipal', 'Guarda Municipal', 'Página da Guarda Municipal', true),('jovem-guarda', 'Jovem Guarda', 'Página da Jovem Guarda', true),('guarda-cidada', 'Guarda Cidadã', 'Página da Guarda Cidadã', true),('rope', 'ROPE', 'Página do ROPE', true),('gmam', 'GMAM', 'Página do GMAM', true),('gsu', 'GSU', 'Página do GSU', true),('defesa-civil', 'Defesa Civil', 'Página da Defesa Civil', true)ON CONFLICT (nome) DO NOTHING;`);console.log('\\n'.concat('='.repeat(80)));console.log('✅ EXECUÇÃO COMPLETA DOS 4 PASSOS');console.log('='.repeat(80));console.log('\\n📋 INSTRUÇÕES FINAIS:');console.log('1. Acesse: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj/database/sql');console.log('2. Copie e cole CADA COMANDO acima, UM POR UM, no SQL Editor');console.log('3. Execute NA ORDEM INDICADA para evitar problemas de dependência');console.log('4. Após executar todos os passos, o sistema estará completamente sincronizado');console.log('\\n💡 DICAS:');console.log('• Execute os comandos em ordem: Passo 1 → Passo 2 → Passo 3 → Passo 4 → Passo Extra');console.log('• Espere cada comando terminar completamente antes de executar o próximo');console.log('• Após a sincronização, os erros de "tabela não encontrada" serão resolvidos');console.log('\\n🎉 PRONTO! O banco de dados está configurado com todas as funcionalidades solicitadas.');