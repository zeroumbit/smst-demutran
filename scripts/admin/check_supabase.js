// Script simplificado para verificar a conexão com o Supabaseimport { createClient } from '@supabase/supabase-js';// Configuração do cliente Supabaseconst SUPABASE_URL = process.env.VITE_SUPABASE_URL || 'https://jpztntmwmrhdobxsyulj.supabase.co';const SUPABASE_ANON_KEY = process.env.VITE_SUPABASE_ANON_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1weHpwcnFxaGNkaXV0bWlubXNmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA3MDk4OTMsImV4cCI6MjA3NjI4NTg5M30.JLTwZjIpw_o6aqeTlb-0q7DqwbVehGIEl6myYpf1Mdw';// Inicializa o cliente Supabaseconst supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);async function testConnection() {  console.log('🚀 Iniciando teste de conexão com o Supabase...');  console.log('   Projeto: jpztntmwmrhdobxsyulj');  console.log('   URL:', SUPABASE_URL);  try {    // Testa a conexão com uma operação simples    console.log('\n🔍 Verificando se a tabela "documentos" existe...');        const { data, error } = await supabase      .from('documentos')      .select('id')      .limit(1);    if (error) {      if (error.code === '42P01' || error.message.includes('does not exist')) {        console.log('❌ Tabela "documentos" não existe no banco de dados');        console.log('👉 Você precisa criar a tabela executando o script SQL:');        console.log('   Copie o conteúdo do arquivo create_documentos_table.sql');        console.log('   e execute no SQL Editor do Supabase Studio');        console.log('   URL: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj/database/sql');      } else {        console.log('⚠️  Erro ao acessar a tabela "documentos":', error.message);      }      return false;    } else {      console.log('✅ Tabela "documentos" existe e está acessível');      console.log('📊 Encontrados', data.length, 'registros na tabela');      return true;    }  } catch (err) {    console.error('❌ Erro durante o teste de conexão:', err.message);    return false;  }}async function checkAllTables() {  console.log('\n📋 Verificando outras tabelas importantes...');    const tablesToCheck = [    'galeria_fotos',    'paginas',    'equipe',    'banners',    'contatos',    'eventos',    'noticias',    'projetos'  ];  for (const table of tablesToCheck) {    try {      const { data, error } = await supabase        .from(table)        .select('id')        .limit(1);      if (error) {        if (error.code === '42P01' || error.message.includes('does not exist')) {          console.log(`   ❌ Tabela "${table}" não existe`);        } else {          console.log(`   ⚠️  Erro ao acessar "${table}":`, error.message);        }      } else {        console.log(`   ✅ Tabela "${table}" existe (${data.length} registros)`);      }    } catch (err) {      console.log(`   ❌ Erro ao verificar "${table}":`, err.message);    }  }}async function main() {  console.log('🔄 Iniciando verificação do ambiente Supabase...\n');    const documentosTableExists = await testConnection();    if (documentosTableExists) {    await checkAllTables();  }    console.log('\n✅ Verificação concluída.');    if (!documentosTableExists) {    console.log('\n💡 Para resolver o problema da tabela "documentos":');    console.log('   1. Acesse: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj/database/sql');    console.log('   2. Copie o conteúdo do arquivo create_documentos_table.sql');    console.log('   3. Cole no editor SQL e execute');  }}// Executa o scriptmain().catch(console.error);