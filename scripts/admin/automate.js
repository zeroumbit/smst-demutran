#!/usr/bin/env node/** * Script principal de automação para integração com Supabase e GitHub * Executa todos os passos necessários para configurar e sincronizar o projeto */const fs = require('fs');const path = require('path');const { execSync } = require('child_process');function runCommand(command, description) {  console.log(`\n${description}`);  console.log(`$ ${command}`);    try {    const output = execSync(command, { encoding: 'utf-8', stdio: 'pipe' });    console.log('✅ Comando executado com sucesso');    return output;  } catch (error) {    console.log('❌ Erro ao executar comando:', error.message);    return null;  }}async function installDependencies() {  console.log('📦 Instalando dependências do projeto...');    if (!fs.existsSync('package.json')) {    console.error('❌ Arquivo package.json não encontrado!');    return false;  }    try {    execSync('npm install', { stdio: 'inherit' });    console.log('✅ Dependências instaladas com sucesso!');    return true;  } catch (error) {    console.error('❌ Erro ao instalar dependências:', error.message);    return false;  }}async function main() {  console.log('🚀 Iniciando processo de automação completo...');  console.log('   Projeto: site-smst');  console.log('   Destino: Supabase Cloud e GitHub');    // Passo 1: Instalar dependências  const depsInstalled = await installDependencies();  if (!depsInstalled) {    console.log('⚠️  Continuando sem instalar dependências...');  }    // Passo 2: Executar o setup de integração com Supabase  console.log('\n📋 Executando setup de integração com Supabase...');  try {    const setupOutput = execSync('npm run setup', { encoding: 'utf-8' });    console.log('✅ Setup de integração concluído');  } catch (error) {    console.log('⚠️  Erro no setup de integração (isso pode ser normal):', error.message);  }    // Passo 3: Executar a integração automática  console.log('\n🔄 Executando integração automática com Supabase...');  try {    require('./auto_integrate.js');    console.log('✅ Integração automática concluída');  } catch (error) {    console.log('⚠️  Erro na integração automática (isso pode ser normal):', error.message);  }    // Passo 4: Tentar sincronizar com o Supabase (verificar status da tabela)  console.log('\n☁️  Verificando sincronização com o Supabase Cloud...');  try {    require('./rest_sync.js');    console.log('✅ Verificação de sincronização com Supabase concluída');  } catch (error) {    console.log('⚠️  Erro na verificação de sincronização:', error.message);  }    // Passo 5: Fornecer instruções finais  console.log('\n🎉 Processo de automação concluído!');  console.log('\n📋 PRÓXIMOS PASSOS:');  console.log('   1. Se o Supabase CLI estiver instalado, execute: npm run sync-supabase');  console.log('   2. Para sincronizar com GitHub, execute o script github_sync.bat (Windows) ou github_sync.sh (Unix)');  console.log('   3. Acesse o painel do Supabase para verificar se a tabela "documentos" foi criada');  console.log('   4. Teste o módulo de documentos no painel administrativo do sistema');    console.log('\n🔗 Recursos:');  console.log('   - Dashboard Supabase: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj');  console.log('   - Repositório GitHub: https://github.com/zeroumbit/site-smst');  console.log('   - Documentação da sincronização: SUPABASE_INTEGRATION.md e GITHUB_SYNC.md');    console.log('\n✅ O módulo de documentos está configurado e pronto para uso!');  console.log('   Após a criação da tabela no Supabase, o sistema funcionará corretamente.');}// Executa o scriptif (require.main === module) {  main().catch(console.error);}module.exports = { main, installDependencies, runCommand };