#!/usr/bin/env node/** * Script para executar o script de criação da tabela documentos diretamente via API do Supabase */// Vamos usar fetch para chamar a API REST do Supabase diretamente// Como não temos o fetch disponível no Node.js padrão, vamos usar o módulo node-fetchconst SUPABASE_URL = 'https://jpztntmwmrhdobxsyulj.supabase.co';const SUPABASE_SERVICE_ROLE_KEY = 'process.env.SUPABASE_SERVICE_ROLE_KEY';// Script SQL para criar a tabela documentosconst createTableSQL = `-- Criação da tabela de documentosCREATE TABLE IF NOT EXISTS documentos (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    nome VARCHAR(255) NOT NULL,    descricao TEXT,    arquivo_url TEXT NOT NULL,    local_exibicao VARCHAR(50) DEFAULT 'secretaria',    ativo BOOLEAN DEFAULT true,    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW());-- Criação do índice para ordenação por data de criaçãoCREATE INDEX IF NOT EXISTS idx_documentos_created_at ON documentos(created_at DESC);-- Criação do índice para status ativoCREATE INDEX IF NOT EXISTS idx_documentos_ativo ON documentos(ativo);-- Criação do índice para local de exibiçãoCREATE INDEX IF NOT EXISTS idx_documentos_local_exibicao ON documentos(local_exibicao);-- Configuração de política de acesso para a tabela documentos-- Permite leitura pública para documentos ativosCREATE POLICY "Permitir leitura de documentos ativos" ON documentos    FOR SELECT TO authenticated, anon    USING (ativo = true);-- Permite todas as operações para authenticated usersCREATE POLICY "Permitir operações CRUD para usuários autenticados" ON documentos    USING (auth.role() = 'authenticated');-- Permite todas as operações para service_roleCREATE POLICY "Permitir operações CRUD para service_role" ON documentos    USING (auth.role() = 'service_role');-- Configuração de storage para documentos-- Criar bucket para documentos se não existirINSERT INTO storage.buckets (id, name, public, avif_autodetection, file_size_limit, allowed_mime_types)VALUES ('documentos', 'documentos', true, false, 10485760,         '{application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document}')ON CONFLICT (id) DO NOTHING;-- Configuração de políticas para o bucket de documentosCREATE POLICY "Permitir leitura pública de documentos" ON storage.objects    FOR SELECT TO authenticated, anon    USING (bucket_id = 'documentos');CREATE POLICY "Permitir upload de documentos para usuários autenticados" ON storage.objects    FOR INSERT TO authenticated    WITH CHECK (bucket_id = 'documentos');CREATE POLICY "Permitir atualização de documentos para usuários autenticados" ON storage.objects    FOR UPDATE TO authenticated    USING (bucket_id = 'documentos');CREATE POLICY "Permitir deleção de documentos para usuários autenticados" ON storage.objects    FOR DELETE TO authenticated    USING (bucket_id = 'documentos');`;async function executeSQLViaRestAPI() {  console.log('🔧 Tentando executar script SQL via API REST do Supabase...');  // Para executar SQL arbitrário via API REST, precisamos usar o endpoint rpc  // mas o Supabase não expõe diretamente um endpoint para executar DDL arbitrário via REST  // A alternativa é usar a interface do Supabase Studio    console.log('ℹ️  Devido a limitações de segurança, o Supabase não permite executar');  console.log('    comandos DDL (como CREATE TABLE) diretamente via API REST.');  console.log('');  console.log('✅ A melhor abordagem é usar o Supabase Studio:');  console.log('');  console.log('1. Acesse o Supabase Studio: https://supabase.com/dashboard/project/jpztntmwmrhdobxsyulj/database/sql');  console.log('2. Clique em "SQL Editor" no menu esquerdo');  console.log('3. Cole o seguinte script SQL:');  console.log('');  console.log('--- INÍCIO DO SCRIPT SQL ---');  console.log(createTableSQL);  console.log('--- FIM DO SCRIPT SQL ---');  console.log('');  console.log('4. Clique em "RUN" para executar o script');  console.log('');  console.log('Isso criará a tabela "documentos" e todos os índices/políticas necessários.');    return true;}async function verifyTableExists() {  console.log('🔍 Verificando se a tabela documentos existe...');    // Vamos tentar usar a biblioteca Supabase JS para verificar se a tabela existe  try {    const { createClient } = await import('@supabase/supabase-js');        // Inicializa o cliente Supabase com a chave de serviço    const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, {      auth: {        autoRefreshToken: false,        persistSession: false,      }    });        // Tenta fazer uma consulta simples para verificar se a tabela existe    const { data, error } = await supabase      .from('documentos')      .select('id')      .limit(1);        if (error) {      if (error.code === '42P01') {        console.log('❌ Tabela "documentos" não existe no banco de dados');        return false;      } else {        console.log(`⚠️ Erro ao verificar tabela: ${error.message}`);        return null; // Não foi possível determinar      }    } else {      console.log('✅ Tabela "documentos" já existe no banco de dados');      return true;    }  } catch (error) {    console.error('❌ Erro ao tentar verificar a tabela:', error.message);    return null; // Não foi possível determinar  }}async function main() {  console.log('🚀 Iniciando verificação e sincronização com o Supabase...');  console.log('   Projeto: jpztntmwmrhdobxsyulj');  console.log('   URL: https://jpztntmwmrhdobxsyulj.supabase.co');    // Primeiro, verifica se a tabela já existe  const tableExists = await verifyTableExists();    if (tableExists === true) {    console.log('\n✅ Banco de dados já está sincronizado!');    console.log('✅ A tabela "documentos" já existe no Supabase');    console.log('✅ O módulo de documentos no painel admin está pronto para uso');  } else if (tableExists === false) {    // A tabela não existe, então precisamos criá-la    console.log('\n❌ A tabela "documentos" ainda não existe no banco de dados.');    await executeSQLViaRestAPI();  } else {    // Houve um erro na verificação, mas vamos tentar a criação mesmo assim    console.log('\n⚠️ Não foi possível determinar se a tabela existe.');    console.log('💡 Recomenda-se verificar manualmente ou executar o script SQL via Studio.');    await executeSQLViaRestAPI();  }}// Executa o scriptif (require.main === module) {  main().catch(console.error);}module.exports = {  executeSQLViaRestAPI,  verifyTableExists};