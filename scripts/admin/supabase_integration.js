// Script de integração com a API do Supabase// Para executar: node supabase_integration.jsimport { createClient } from '@supabase/supabase-js';// Configuração do cliente Supabaseconst SUPABASE_URL = process.env.VITE_SUPABASE_URL;const SUPABASE_ANON_KEY = process.env.VITE_SUPABASE_ANON_KEY;if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {  console.error('ERRO: Defina VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY no .env');  process.exit(1);}// Inicializa o cliente Supabase com a chave anon para testes iniciaisconst supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {  auth: {    autoRefreshToken: false,    persistSession: false,  }});